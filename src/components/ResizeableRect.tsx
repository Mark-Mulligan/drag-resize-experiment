const ResizeableRect = () => {
  return (
    <div style={{ position: 'absolute', top: 0, left: 0, height: 100, width: 200, border: '2px solid green' }}></div>
  );
};

export default ResizeableRect;
